Tracking the following bugs in the PCD Community Edition deployment:
- PCD-9757: airctl CLI is not functional in PCD CE 2026.4 after Install without explicit config path
- PCD-9783: terraform destroy does not delete the cluster blueprint
- PCD-9784: TF Provider Docs do not provide enough info to import pcd_blockstorage_volume_type resources
- PCD-9799: PCD TF Provider does not provide a pcd_host data resource
- PCD-9800: PCD CE installer does not expose flag to enable IPV4/IPV6 dual stack networking.
- PCD-9802: pcdctl doesn't recognize systemd-timesyncd as valid NTP
- PCD-9803: pcd TF provider does not handle changes to `pcd_host_config` network labels